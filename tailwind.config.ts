import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        royal: {
          purple: "#6C3BAA",
          gold: "#D4AF37",
          black: "#0B0B0F",
          crimson: "#DC143C",
        },
      },
    },
  },
};

export default config;
